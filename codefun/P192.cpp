#include <bits/stdc++.h>
using namespace std;

int main()
{
    string str;
    getline(cin, str);
    long long length = str.length();
    long long result = length - 1;
    cout << result;
    return 0;
}