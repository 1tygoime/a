#include <bits/stdc++.h>
using namespace std;
int main(){
    unsigned long long n,temp1,tong(0);
    cin >> n;
    if (sqrt(n) == (long long)sqrt(n)) cout << "YES";
    else cout << "NO";
    return 0;
}