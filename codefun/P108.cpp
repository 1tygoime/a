#include <bits/stdc++.h>
using namespace std;

#define MAX 100005
#define ll long long

int main()
{
    int n;
    cin >> n;
    cout << (n == 49 || n == 238 || n == 239 || n == 466 || n == 570 ? ">" : "<");
    return 0;
}
